.Side-bar
  .Side-header
    .Side-header__box
      .Side-header__box__user-name
        = current_user.name
      .Side-header__box__icons
        = link_to new_group_path do 
          = icon('far', 'edit', class: "Side-header__box__icons--edit-icon")
        = link_to edit_user_path(current_user) do 
          = icon('fas', 'cog', class: "Side-header__box__icons--cog-icon")
  .Side-groups
    - current_user.groups.each do |group|
      .Groups__box
        = link_to group_messages_path(group), class:"Groups__box__group" do
          .Groups__box__group__name
            = group.name
          .Groups__box__group__message 
            = group.show_last_message